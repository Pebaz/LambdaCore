(defn 'add-sub ['a 'b 'c] '[
    (print [a b c])
    (if c '[
        (print a)
    ]'[
        (print b)
    ])
])

::(add-sub (print "1") (print "2") (print "3"))
(add-sub 2 4 False)

:: BUG: arguments are added backwards
